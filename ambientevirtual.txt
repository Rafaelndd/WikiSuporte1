# WikiSuporte — dependências do ambiente virtual
#
# Criar venv (raiz do projeto):
#   python -m venv .venv
#
# Ativar no PowerShell:
#   .\.venv\Scripts\Activate.ps1
#
# Instalar tudo:
#   pip install --upgrade pip
#   pip install -r ambientevirtual.txt
#
# Este ficheiro é consumido por pip (-r). Inclui requirements.txt + pacotes extra.
-r requirements.txt
