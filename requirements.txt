# WikiSuporte — dependências diretas do projeto.
# Instalar: pip install -r requirements.txt
# O pip resolve e instala dependências transitivas (Streamlit, google-genai, etc.).

# --- Aplicação ---
streamlit
streamlit-authenticator>=0.4.0
python-dotenv>=1.0.0

# --- Dados e PostgreSQL ---
pandas>=2.0.0
numpy>=1.24.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# --- HTTP (app, APIs, cache) ---
# openmeteo-requests 1.5+ puxa stack HTTP alternativa e pode conflitar com requests/urllib3.
openmeteo-requests>=1.4.0,<1.5.0
requests>=2.31.0,<3
# CVE / alinhamento com CI (pip-audit): manter urllib3 recente.
urllib3>=2.6.3
requests-cache>=1.0.0
retry-requests>=2.0.0

# --- Raspagem / automação ---
selenium>=4.15.0
webdriver-manager
beautifulsoup4>=4.12.0

# --- IA (Gemini / embeddings via google-genai) ---
google-genai>=1.0.0

# --- Documentos ---
pypdf>=4.0.0
openpyxl>=3.0.0
reportlab>=4.0.0
python-docx>=1.0.0
striprtf>=0.0.26

# --- Embeddings opcionais (EMBEDDING_MODEL=openai no .env) ---
openai>=1.0.0

# --- Sistema (bot, página de configurações) ---
psutil>=5.9.0

# --- Gráficos ---
plotly>=5.18.0

# --- Criptografia / autenticação (extras do streamlit-authenticator, senhas, TLS) ---
bcrypt
cryptography
