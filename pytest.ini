[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requer banco ou rede (não incluído no gate padrão)
    streamlit_smoke: carrega páginas Streamlit com mocks (sem servidor)
